/* The default config file, as used with the HDK, is fully auto-detected, so no configuration required. */
/* See the JSON-Schema for server config or other samples to see possible options. */
{
  "display": "displays/LG_G4.json",
  "plugins": [
    "com_osvr_android_jniImaging",
    "com_osvr_android_sensorTracker",
    "com_osvr_Multiserver"
  ],
  "externalDevices": {
    "/myExternalDevice": {
      "deviceName": "Tracker0",
      "server": "192.168.56.1:3884",
      "descriptor": {
        "interfaces": {
          "tracker": {},
          "button": {},
          "analog": {}
        },
        "Note": "can also provide a file path here, or this is the minimal"
      }
    }
  },
  "aliases": {
    "/me/head": "/myExternalDevice/tracker/0"
  }
}
